<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DroidShow">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!--
                    Accept archive VIEW intents from file managers, document providers,
                    and browser-like pickers that include BROWSABLE.
                -->

                <data android:scheme="content" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
                <data android:mimeType="application/x-7z-compressed" />
                <data android:mimeType="application/x-rar-compressed" />
                <data android:mimeType="application/vnd.rar" />
                <data android:mimeType="application/x-rar" />
                <data android:mimeType="application/rar" />
                <data android:mimeType="application/octet-stream" />

                <!-- Common comic archive MIME variants from file managers/providers -->
                <data android:mimeType="application/vnd.comicbook+zip" />
                <data android:mimeType="application/x-cbz" />
                <data android:mimeType="application/vnd.comicbook-rar" />
                <data android:mimeType="application/x-cbr" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!--
                    Extension fallback for providers that omit or mislabel MIME type.
                    Keep content:// scoped while allowing BROWSABLE-based resolvers.
                -->

                <data android:scheme="content" android:pathSuffix=".zip" />
                <data android:scheme="content" android:pathSuffix=".ZIP" />
                <data android:scheme="content" android:pathSuffix=".rar" />
                <data android:scheme="content" android:pathSuffix=".RAR" />
                <data android:scheme="content" android:pathSuffix=".7z" />
                <data android:scheme="content" android:pathSuffix=".7Z" />
                <data android:scheme="content" android:pathSuffix=".cbz" />
                <data android:scheme="content" android:pathSuffix=".CBZ" />
                <data android:scheme="content" android:pathSuffix=".cbr" />
                <data android:scheme="content" android:pathSuffix=".CBR" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!--
                    Compatibility fallback for file managers that still expose file:// URIs.
                    We narrow handling to known archive extensions only.
                -->

                <data android:scheme="file" android:pathSuffix=".zip" />
                <data android:scheme="file" android:pathSuffix=".ZIP" />
                <data android:scheme="file" android:pathSuffix=".rar" />
                <data android:scheme="file" android:pathSuffix=".RAR" />
                <data android:scheme="file" android:pathSuffix=".7z" />
                <data android:scheme="file" android:pathSuffix=".7Z" />
                <data android:scheme="file" android:pathSuffix=".cbz" />
                <data android:scheme="file" android:pathSuffix=".CBZ" />
                <data android:scheme="file" android:pathSuffix=".cbr" />
                <data android:scheme="file" android:pathSuffix=".CBR" />
            </intent-filter>
        </activity>
    </application>

</manifest>
